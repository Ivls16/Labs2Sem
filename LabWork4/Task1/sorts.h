#ifndef SORTS_H
#define SORTS_H

#include <random>
#include <chrono>
#include "heap.h"

void merge_sort(int*, int, int);
void quick_sort(int*, int, int);
void heap_sort(int*, int);
void interpolation_sort(int*, int);

#endif //SORTS_H


